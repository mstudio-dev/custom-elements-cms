{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}
    <h1>Medium verschieben: {{ media.name }}</h1>
{% endblock %}

{% block main %}
    <div class="card">
        <div class="card-body">
            <form method="post">
                <div class="mb-3">
                    <label for="target_folder" class="form-label">Zielordner auswählen</label>
                    <select name="target_folder" id="target_folder" class="form-select">
                        <option value="">📁 Root (Kein Ordner)</option>
                        {% for folder in folders %}
                            <option value="{{ folder.id }}" {% if media.parent and media.parent.id == folder.id %}selected{% endif %}>
                                {% if folder.parent %}└─ {% endif %}📁 {{ folder.name }}
                            </option>
                        {% endfor %}
                    </select>
                </div>
                
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-check"></i> Verschieben
                    </button>
                    <a href="{{ ea_url().setAction('index') }}" class="btn btn-secondary">
                        <i class="fa fa-times"></i> Abbrechen
                    </a>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
