{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}
    <h1>{{ entity_ids | length }} Medien verschieben</h1>
{% endblock %}

{% block main %}
    <div class="card">
        <div class="card-body">
            <p class="mb-4">Sie sind dabei, {{ entity_ids | length }} Medien zu verschieben.</p>
            
            <form method="post" action="{{ ea_url().setAction('batchMoveToFolder').set('batchActionUrl', referrer) }}">
                {% for id in entity_ids %}
                    <input type="hidden" name="batchActionEntityIds[]" value="{{ id }}">
                {% endfor %}
                
                <div class="mb-3">
                    <label for="target_folder" class="form-label">Zielordner auswählen</label>
                    <select name="target_folder" id="target_folder" class="form-select" required>
                        <option value="">📁 Root (Kein Ordner)</option>
                        {% for folder in folders %}
                            <option value="{{ folder.id }}">
                                {% if folder.parent %}└─ {% endif %}📁 {{ folder.name }}
                            </option>
                        {% endfor %}
                    </select>
                </div>
                
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-check"></i> Verschieben
                    </button>
                    <a href="{{ referrer }}" class="btn btn-secondary">
                        <i class="fa fa-times"></i> Abbrechen
                    </a>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
